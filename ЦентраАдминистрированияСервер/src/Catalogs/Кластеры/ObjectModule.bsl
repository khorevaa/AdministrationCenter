
Процедура ПередЗаписью(Отказ)
	АдресАгентаСервера = ВРег(АдресАгентаСервера);
	
	ПредставлениеВерсии = "Версия " + Строка(ВерсияПлатформы);
	ПредставлениеАдресаСервера = АдресАгентаСервера;
	ПредставлениеПортаСервера = ПортКластера;
	Наименование = ПредставлениеАдресаСервера + ":" +ПредставлениеПортаСервера 
									+  " (" + ПредставлениеВерсии + ", " + Представление + ")"

КонецПроцедуры
